services:
  postgres:
    image: postgres:16
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: gidi
      POSTGRES_USER: gidi
      POSTGRES_PASSWORD: gidi
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data:
